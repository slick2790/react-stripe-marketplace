import React from 'react';

const Index: React.FC = () => {
  // This component is now just a placeholder since the homepage content 
  // is rendered directly in AppLayout when on the home route
  return null;
};

export default Index;